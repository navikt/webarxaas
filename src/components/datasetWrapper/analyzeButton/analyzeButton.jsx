import React from 'react';
import { Knapp } from 'nav-frontend-knapper';

const AnalyzeButton = (props) => {
    const content = (
        <div>
            <Knapp>Analyze Datasett</Knapp>
        </div>
    );
    return content;
};

export default AnalyzeButton;